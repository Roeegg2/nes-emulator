# stuff to read more about:

cpu/ppu clock allignment.
different scrolling (split scorlling, single scrolling, etc).
$2007 reads and writes | "PPU scrolling".
Master/slave mode and the EXT pins.
Bit 0 race condition.


# stuff to implement:

- grayscale bit on ppumask
- color tint on ppumask
